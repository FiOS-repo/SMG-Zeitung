---
const { active, next, nextnext} = Astro.props;
import Button from "./Button.astro";
---

<div class="bg-blue-200 rounded-3xl bg-clip-padding backdrop-filter backdrop-blur-3xl bg-opacity-20 ring-1 ring-blue-200  p-6 shadow-lg flex items-center justify-center mt-12 space-x-4">
    <Button text="Zurück" id="prev-page"/>

    <!-- Page Numbers -->
    <div class="flex space-x-2" id="pagination-numbers">

        <Button text={ active } class="bg-blue-300"/>
        <Button text={ next }/>
        <Button text={ nextnext }/>

    </div>

    <!-- Next Button -->
    <Button text="Weiter" id="next-page">
        Weiter <i class="fas fa-chevron-right ml-2"></i>
    </button>
</div>
